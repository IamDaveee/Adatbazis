CREATE DATABASE iskola;
----A létrehozás csak egyszer történik meg utána kommentelés nélkül sir h már létezik
CREATE TABLE 12c (nev varchar(20), szuletesi_hely varchar(20), kor SMALLINT);
---amíg nincs kiválasztva a DB addig nem hozza létre USE db;

INSERT INTO 12c (nev, szuletesi_hely, kor) VALUES ('Kristóf', 'Siófok', 19);
INSERT INTO 12c (nev, szuletesi_hely, kor) VALUES ('Bence', 'Pécs', 20);
INSERT INTO 12c (nev, szuletesi_hely, kor) VALUES ('Ádám', 'Siófok', 19);
INSERT INTO 12c (nev, szuletesi_hely, kor) VALUES ('Soma', 'Marcali', 19);
INSERT INTO 12c (nev, szuletesi_hely, kor) VALUES ('Patrik', 'Kaposvár', 18);

-----Lekérdezéseknél mindig a legutolsót futtatja le... érettségin/dolgozatban legyen egy olyan Query oldal amiben futtatás nélkül mented a megoldásokat. egy amiben pedig gyakorlatozol.

SELECT nev FROM 12c;

SELECT * FROM 12c;

SELECT nev,kor
FROM 12c;

SELECT nev, szuletesi_hely
FROM 12c
WHERE szuletesi_hely='Pécs'; (Siófokot is nézzük meg)

SELECT nev, szuletesi_hely
FROM 12c
WHERE szuletesi_hely>'K';
---- módosítjuk a táblát akkor az ALTER  paranccsal fogjuk tudni.

Adjunk hozzá egy az- azonosítót amit beállítunk elsõdleges kulcsnak.
ALTER TABLE 12c
ADD (az INT PRIMARY KEY AUTO_INCREMENT);

UPDATE 12c
SET nev='Bence'
WHERE nev='Bercel';